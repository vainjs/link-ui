# 快速开始

全量注册：

```ts
import { createApp } from 'vue'
import LinkUI from '@scope/link-ui'
import '@scope/link-ui/dist/style.css'
import App from './App.vue'

createApp(App).use(LinkUI).mount('#app')
```

具体组件会在后续 OpenSpec change 中逐个实现。
